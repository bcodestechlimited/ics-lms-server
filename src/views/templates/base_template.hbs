<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{title}}</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&display=swap"
    rel="stylesheet">
  <style>
    body {
      font-family: 'Playfair Display', serif;
      margin: 0;
      padding: 0;
      background: #ffffff;
      color: #333;
    }

    a {
      text-decoration: none;
      color: #FF502A;
      font-weight: 500;
    }

    p {
      text-align: left;
    }

    hr {
      outline: none;
      border-top: dashed 0.5px #5B5B5D;
    }

    .container {
      width: 100%;
      margin: 0 auto;
      padding: 20px;
      border-radius: 8px;
    }

    .header {
      margin-bottom: 20px;
      padding: 20px;
      background-color: rgba(225, 214, 214, 0.45);
      text-align: center;
    }

    .header img {
      max-width: 150px;
      margin-top: 13px;
      margin-right: 7px;
    }

    .header-text {
      color: #393938;
      font-family: "FONTSPRING DEMO - Roc Grotesk";
      font-size: 24px;
      font-weight: 700;
    }

    .content {
      text-align: center;
      padding: 70px 40px;
      margin: 0 0;
    }

    .content h1 {
      font-size: 24px;
      margin-bottom: 20px;
    }

    .content p {
      font-size: 14px;
      line-height: 1.5;
      margin-bottom: 20px;
      text-align: left;
    }

    .button-container {
      margin: 20px 0;
    }

    .button-container a {
      background-color: #FF502A;
      color: #fff;
      text-decoration: none;
      padding: 8px 30px;
      border-radius: 5px;
      font-size: 13px;
      margin-top: 12px;
    }

    .features {
      text-align: left;
      margin-top: 20px;
      list-style: none;
    }

    .features li {
      color: #333333;
      font-size: 16px;
      margin-bottom: 10px;
      list-style: none;
    }

    .footer {
      background-color: rgba(225, 214, 214, 0.45);
      text-align: center;
      font-size: 12px;
      color: #999;
    }

    .social-icons img {
      margin: 0 5px;
      width: 32px;
      height: 32px;
    }

    .unsubscribe {
      margin-top: 20px;
    }
  </style>
</head>

<body>
  <div class="container">
    <table class="header" role="presentation" width="100%" cellpadding="0" cellspacing="0">
      <tr>
        <td align="center">
          <img src="{{logoUrl}}" alt="ICS Academy logo" style="max-width: 150px;" />
          <span class="header-text">ICS Academy</span>
        </td>
      </tr>
    </table>

    <div class="content">
      {{> content}}
      <p style="margin:20px 0 0 0 ;"><strong>Regards,<br />{{companyName}}</strong></p>
    </div>

    <div class="footer">
      <div class="social-icons">
        {{#each socialIcons}}
        <a href="{{this.url}}"><img src="{{this.imgSrc}}" alt="{{this.alt}}" /></a>
        {{/each}}
      </div>

      <p>Thank you for choosing {{companyName}}. Need help? <a href="{{supportUrl}}">Contact our customer support</a>
      </p>

      <hr />

      <div class="unsubscribe">
        <p>You are receiving this email because you signed up at {{companyName}}. Want to change how you receive these
          emails?</p>
      </div>
    </div>
  </div>
</body>

</html>